# Early Access support communication templates

Primary customer language is Dutch. Replace placeholders with confirmed facts only.

## 1. Ontvangstbevestiging

We hebben je melding ontvangen onder referentie `<SUP-ID>`. We controleren de impact en komen terug met een volgende update zodra er bevestigde informatie is. Deel geen wachtwoorden, JWT's, sessietokens of API-sleutels.

## 2. Onderzoek

We onderzoeken momenteel een probleem met `<onderdeel>`. De huidige impact is `<impact>`. We voeren veilige controles uit en geven een volgende update rond `<volgende-update>`.

## 3. Hersteld

Het gemelde probleem lijkt hersteld. We hebben dit gecontroleerd met de beschikbare health- en read-only controles. Laat het weten als het opnieuw optreedt onder `<SUP-ID>`.

## 4. Meer informatie nodig

Kun je organisatie, tijdstip, pagina/actie, verwacht gedrag, werkelijk gedrag, browser/apparaat en een veilige screenshot delen? Stuur geen wachtwoorden, JWT's, sessietokens, API-sleutels of volledige klantdocumenten.

## 5. Bekend probleem / workaround

Dit probleem is bekend bij `<onderdeel>`. Gebruik voorlopig `<veilige-workaround>`. We volgen dit op onder `<SUP-ID>` en communiceren nieuwe bevestigde informatie zodra die beschikbaar is.

## 6. Security/privacy

We hebben je melding geregistreerd onder `<INC-ID>` als mogelijk security- of privacy-incident. We beperken verdere acties en bewaren relevante evidence. Deel geen credentials, tokens of volledige klantdocumenten. Eventuele juridische beoordeling volgt via de aangewezen privacy/legal owner.

Deze teksten zijn geen contractuele SLA, juridische verklaring of toezegging van een oplossingstermijn.
