import Link from "next/link";
import { Building2, LayoutDashboard, ListTodo, LogOut, MessageSquare, FileText, Package, ReceiptText, Settings, Users } from "lucide-react";
export default async function CompanyLayout({ children, params }: { children: React.ReactNode; params: Promise<{ companySlug: string }> }) { const { companySlug } = await params; const root = `/app/${companySlug}`; return <div className="min-h-screen"><header className="border-b bg-white"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-6"><Link href="/" className="flex items-center gap-2 font-semibold"><Building2 className="text-blue-700" /> AI FlowOS</Link><form action="/api/auth/signout" method="post"><button className="inline-flex items-center gap-2 text-sm text-slate-600 hover:text-slate-950"><LogOut size={16} /> Uitloggen</button></form></div></header><div className="mx-auto grid max-w-7xl gap-8 px-6 py-8 md:grid-cols-[190px_1fr]"><aside><nav className="space-y-1 text-sm"><Nav href={root} icon={LayoutDashboard} label="Overzicht" /><Nav href={`${root}/quotes`} icon={FileText} label="Offertes" /><Nav href={`${root}/invoices`} icon={ReceiptText} label="Facturen" /><Nav href={`${root}/catalog`} icon={Package} label="Producten" /><Nav href={`${root}/conversations`} icon={MessageSquare} label="Aanvragen" /><Nav href={`${root}/tasks`} icon={ListTodo} label="Taken" /><Nav href={`${root}/team`} icon={Users} label="Team" /><Nav href={`${root}/settings`} icon={Settings} label="Huisstijl" /><Nav href={`${root}/billing`} icon={ReceiptText} label="Abonnement" /></nav></aside><main>{children}</main></div></div>; }
function Nav({ href, icon: Icon, label }: { href: string; icon: typeof LayoutDashboard; label: string }) { return <Link href={href} className="flex items-center gap-2 rounded-md px-3 py-2 text-slate-700 hover:bg-slate-100"><Icon size={16} />{label}</Link>; }
