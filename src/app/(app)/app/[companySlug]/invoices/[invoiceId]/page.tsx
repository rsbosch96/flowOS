import { notFound } from "next/navigation";
import { Card } from "@/components/ui/card";
import { createClient } from "@/lib/supabase/server";
import { DownloadInvoicePdfButton } from "@/features/invoices/components/download-invoice-pdf-button";
const euro = (cents: number) => new Intl.NumberFormat("nl-NL", { style: "currency", currency: "EUR" }).format(cents / 100);
export default async function InvoicePage({ params }: { params: Promise<{ companySlug: string; invoiceId: string }> }) { const { companySlug, invoiceId } = await params; const supabase = await createClient(); const { data: company } = await supabase.from("companies").select("id,name").eq("slug", companySlug).maybeSingle(); if (!company) notFound(); const { data: invoice } = await supabase.from("invoices").select("invoice_number,status,due_at,subtotal_cents,tax_cents,total_cents,notes,customers(name,email),invoice_items(position,description,quantity,unit,unit_price_cents,vat_rate,line_total_cents)").eq("id", invoiceId).eq("company_id", company.id).maybeSingle(); if (!invoice) notFound(); const items = invoice.invoice_items as unknown as Array<{ position: number; description: string; quantity: number; unit: string; unit_price_cents: number; vat_rate: number; line_total_cents: number }>; return <div className="max-w-3xl"><Card><div className="flex items-start justify-between gap-4"><p className="text-sm text-slate-600">{company.name}</p><DownloadInvoicePdfButton companyId={company.id} invoiceId={invoiceId} /></div><div className="mt-2 flex justify-between gap-4"><div><h1 className="text-2xl font-semibold">Factuur {invoice.invoice_number}</h1><p className="mt-1 text-sm text-slate-600">Voor {(invoice.customers as unknown as { name: string } | null)?.name}</p></div><p className="rounded-full bg-slate-100 px-3 py-1 text-sm">{invoice.status}</p></div><p className="mt-3 text-sm text-slate-600">Betaaltermijn: {invoice.due_at ?? "nog niet ingesteld"}</p><div className="mt-6 divide-y border-y">{items.sort((a, b) => a.position - b.position).map((item) => <div className="flex justify-between gap-4 py-3" key={item.position}><div><p>{item.description}</p><p className="text-sm text-slate-600">{item.quantity} {item.unit} x {euro(item.unit_price_cents)} · {item.vat_rate}% btw</p></div><p className="font-medium">{euro(item.line_total_cents)}</p></div>)}</div><dl className="ml-auto mt-6 max-w-xs space-y-2"><div className="flex justify-between"><dt>Subtotaal</dt><dd>{euro(invoice.subtotal_cents)}</dd></div><div className="flex justify-between"><dt>BTW</dt><dd>{euro(invoice.tax_cents)}</dd></div><div className="flex justify-between border-t pt-2 text-lg font-semibold"><dt>Totaal</dt><dd>{euro(invoice.total_cents)}</dd></div></dl>{invoice.notes && <p className="mt-6 whitespace-pre-wrap text-sm text-slate-700">{invoice.notes}</p>}</Card></div>; }
