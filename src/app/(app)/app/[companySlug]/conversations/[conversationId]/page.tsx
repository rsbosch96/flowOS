import Link from "next/link";
import { notFound } from "next/navigation";
import { Card } from "@/components/ui/card";
import { createClient } from "@/lib/supabase/server";
import { ConversationActions } from "@/features/conversations/components/conversation-actions";
export default async function ConversationPage({ params }: { params: Promise<{ companySlug: string; conversationId: string }> }) { const { companySlug, conversationId } = await params; const supabase = await createClient(); const { data: company } = await supabase.from("companies").select("id").eq("slug", companySlug).maybeSingle(); if (!company) notFound(); const { data: conversation } = await supabase.from("conversations").select("subject,status,channel,customers(name,email),conversation_messages(direction,body,created_at)").eq("id", conversationId).eq("company_id", company.id).maybeSingle(); if (!conversation) notFound(); const messages = (conversation.conversation_messages as unknown as Array<{ direction: string; body: string; created_at: string }>).sort((a, b) => a.created_at.localeCompare(b.created_at)); const customer = conversation.customers as unknown as { name: string; email: string | null } | null; return <div className="max-w-3xl space-y-4"><Link className="text-sm text-blue-700 hover:underline" href={`/app/${companySlug}/conversations`}>← Terug naar aanvragen</Link><Card><div className="flex justify-between gap-4"><div><p className="text-sm text-slate-600">{customer?.name} {customer?.email ? `· ${customer.email}` : ""}</p><h1 className="mt-1 text-xl font-semibold">{conversation.subject ?? "Zonder onderwerp"}</h1></div><p className="rounded-full bg-slate-100 px-3 py-1 text-sm">{conversation.status}</p></div><div className="mt-6 space-y-3">{messages.map((message, index) => <div className={message.direction === "inbound" ? "rounded-lg bg-blue-50 p-3" : "rounded-lg bg-slate-100 p-3"} key={`${message.created_at}-${index}`}><p className="text-xs font-medium uppercase text-slate-500">{message.direction === "inbound" ? "Klant" : "Interne notitie"}</p><p className="mt-1 whitespace-pre-wrap text-sm">{message.body}</p></div>)}</div><ConversationActions companyId={company.id} conversationId={conversationId} status={conversation.status} /></Card></div>; }
